<!DOCTYPE html>
<html>
    <head>
        <script type="text/javascript" src="/js/jquery-3.3.1.min.js"></script>
        <script type="text/javascript" src="/js/bootstrap.min.js"></script>
        <link type="text/css" rel="stylesheet" href="/css/bootstrap.css">
        <link type="text/css" rel="stylesheet" href="/css/file.css">
        <meta charset="UTF-8">
        <script>
            function file_change(file) {
                var str=file.lastIndexOf("\\")+1;	//파일 마지막 "\" 루트의 길이 이후부터 글자를 잘라 파일명만 가져온다.
                console.log(str);
                file = file.substring(str, file.length);
                document.getElementById('fileName').value=file;
            }
        </script>
        <title>Shuttles Notice Management Page</title> 
    </head>
    <body>
        <article class="container">
            <div class="page-header">
                <div class="col-md-6 col-md-offset-3">
                    <h3>Shuttles 공지사항 수정 페이지</h3>
                </div>
            </div>
            <div class="col-sm-6 col-md-offset-3">
                <form method="post" enctype="multipart/form-data" action="/notice/update">
                    <div class="form-group">
                        <label for="subject">제목</label>
                        <input type="text" class="form-control" id="notice_subject" name="notice_subject" placeholder="제목을 입력해 주세요" value=<%= result[0].notice_subject%>>
                    </div>
                    <div class="form-group">
                        <label for="content">내용</label>
                        <textarea class="form-control" id="notice_content" name="notice_content" cols='40' rows='10'><%= result[0].notice_content %></textarea>
                    </div>
                    <div class="form-group">
                        <label for="inputPicture">사진</label>
                        <!--<input type="file" name="notice_picture">-->
                        <div class="file_input_div">
                            <input type="button" value="파일 선택" class="file_input_button"/>
                            <input type="file" id="notice_picture" name="notice_picture" class="file_input_hidden" 
                            onchange="javascript:file_change(this.value);"/>
                        </div>
                        <input id="fileName" class="file_input_textbox" readonly value=<%= result[0].notice_picture%>>
                    </div>
                    <div class="form-group text-center">
                        <button type="submit" id="notice_submit" class="btn btn-primary">
                            메뉴수정<i class="fa fa-check spaceLeft"></i>
                        </button>
                        <button type="button" class="btn btn-warning" onclick='history.back()'>
                            뒤로가기<i class="fa fa-times spaceLeft"></i>
                        </button>
                    </div>
                    <input type='hidden' name='notice_id' value=<%= result[0].notice_id %>>
                </form>
            </div>
        </article>
    </body>
</html>